// Local, disposable MongoDB. Never loads production .env or connects to Atlas.
import { MongoMemoryReplSet } from 'mongodb-memory-server';
import mongoose from 'mongoose';
import express from 'express';
import jwt from 'jsonwebtoken';
import assert from 'node:assert/strict';
import { randomBytes } from 'node:crypto';
import User from '../server/models/User.js';
import Module from '../server/models/Module.js';
import Lesson from '../server/models/Lesson.js';
import Material from '../server/models/Material.js';
import Exam from '../server/models/Exam.js';
import Question from '../server/models/Question.js';
import ExamAttempt from '../server/models/ExamAttempt.js';
import StudentProgress from '../server/models/StudentProgress.js';
import studentRoutes from '../server/routes/studentRoutes.js';
import adminRoutes from '../server/routes/adminRoutes.js';
import authRoutes from '../server/routes/authRoutes.js';
import FinalAuthorization from '../server/models/FinalAuthorization.js';
process.env.JWT_SECRET=randomBytes(32).toString('hex');
process.env.STORAGE_ENABLED='false';delete process.env.STORAGE_SECRET;delete process.env.STORAGE_URL;
const mongo=await MongoMemoryReplSet.create({replSet:{count:1, storageEngine:'wiredTiger'}});let server;
try {
  await mongoose.connect(mongo.getUri(),{dbName:'inavet_local_test'});
  await Promise.all([Exam.init(), Question.init(), FinalAuthorization.init()]);
  const makeUser=(username,role,status='ACTIVE')=>User.create({username,role,status,firstName:'Prueba',lastName:username,dni:username,email:username+'@example.invalid',passwordHash:'Local-test-only-2026!',mustChangePassword:false});
  const admin=await makeUser('admin_test','ADMIN'), student=await makeUser('student_test','STUDENT'), inactive=await makeUser('inactive_test','STUDENT','INACTIVE');
  const [a,b,h]=await Module.create([{title:'Anatomía',order:1},{title:'Primeros auxilios',order:2},{title:'Oculto',status:'INACTIVE'}]);
  const [a1,a2,b1,b2,h1]=await Lesson.create([{title:'Clase 1',moduleId:a._id,order:4},{title:'Sistema óseo',moduleId:a._id,order:8},{title:'Introducción',moduleId:b._id,order:10},{title:'Cuidados',moduleId:b._id,order:20},{title:'Oculta',moduleId:h._id}]);
  const materials=await Material.create([a1,a2,b1,b2].map(l=>({title:'Apunte '+l.title,lessonId:l._id,type:'TEXT',content:'Material ficticio'})));
  const exams=await Exam.create([a1,a2,b1,b2].map(l=>({title:'Examen '+l.title,lessonId:l._id})));
  const final=await Exam.create({title:'Examen final de Anatomía',moduleId:a._id,passingScorePercent:80,maxAttemptsPerAuthorization:1});
  for(const ex of [...exams,final])await Question.create({examId:ex._id,prompt:'Elegí la correcta',options:['Correcta','Incorrecta'],correctOptionIndex:0});
  const app=express();app.use(express.json());app.use('/api/auth',authRoutes);app.use('/api/student',studentRoutes);app.use('/api/admin',adminRoutes);
  server=app.listen(process.env.QA_KEEP_OPEN==='true'?5000:0,'127.0.0.1');await new Promise(r=>server.once('listening',r));
  const base='http://127.0.0.1:'+server.address().port;
  async function req(path,method='GET',body,user=student,status=200){const r=await fetch(base+path,{method,headers:{'Content-Type':'application/json',...(user?{Authorization:'Bearer '+jwt.sign({id:String(user._id)},process.env.JWT_SECRET)}:{})},body:body===undefined?undefined:JSON.stringify(body)});const d=await r.json();assert.equal(r.status,status,`${path}: ${JSON.stringify(d)}`);return d.data;}
  const dashboard=()=>req('/api/student/dashboard');
  const submit=async(ex,correct=true,status=200)=>{const q=await Question.findOne({examId:ex._id});return req('/api/student/exam/'+ex._id+'/submit','POST',{answers:[{questionId:String(q._id),selectedOptionIndex:correct?0:1}]},student,status);};
  const mark=(l,m,status=200)=>req(`/api/student/lesson/${l._id}/material/${m._id}/toggle-view`,'POST',undefined,student,status);
  assert.equal((await dashboard()).modules.length,2);
  assert.equal((await dashboard()).continueWhereLeft.moduleTitle, 'Anatomía');
  await req('/api/student/dashboard','GET',undefined,null,401);await req('/api/student/dashboard','GET',undefined,inactive,403);
  await req('/api/student/lesson/'+a1._id);await req('/api/student/lesson/'+b1._id);
  await req('/api/student/lesson/'+a2._id,'GET',undefined,student,403);await req('/api/student/lesson/'+h1._id,'GET',undefined,student,403);
  await req('/api/student/exam/'+exams[1]._id,'GET',undefined,student,403);await submit(exams[1],true,403);await submit(final,true,403);
  assert.equal(await ExamAttempt.countDocuments(),0);await mark(a1,materials[2],404);
  await req('/api/student/exam/'+exams[0]._id,'GET',undefined,student,403);
  await submit(exams[0],true,403);
  const additional = await Material.create({title:'Complementario',lessonId:a1._id,type:'TEXT',required:false});
  await mark(a1,materials[0]);
  await submit(exams[0],true,403); // ALL current material, not only required material.
  await mark(a1,additional);
  await req('/api/student/exam/'+exams[0]._id);
  assert.equal((await submit(exams[0])).passed,true);
  await mark(a1,additional); await submit(exams[0],true,403); await mark(a1,additional);
  await req('/api/student/lesson/'+a2._id);await req('/api/student/lesson/'+b2._id,'GET',undefined,student,403);
  await mark(a2,materials[1]);await submit(final,true,403);assert.equal((await submit(exams[1],false)).passed,false);await submit(final,true,403);await submit(exams[1]);
  assert.equal((await dashboard()).modules[0].finalExam.status,'AVAILABLE');
  await submit(final,false,403);
  const requestPath='/api/student/final-exams/'+final._id+'/request';
  await req(requestPath,'POST'); await req(requestPath,'POST');
  assert.equal(await FinalAuthorization.countDocuments(),1);
  const grant=await FinalAuthorization.findOne({studentId:student._id,examId:final._id});
  const approvePath='/api/admin/final-authorizations/'+grant._id+'/approve';
  await req(approvePath,'POST',undefined,student,403);
  await req(approvePath,'POST',undefined,admin);
  await req(approvePath,'POST',undefined,admin,400);
  await req('/api/admin/exams/'+final._id,'PUT',{maxAttemptsPerAuthorization:2},admin);
  assert.equal((await FinalAuthorization.findById(grant._id)).attemptLimit,1);
  // Two parallel submissions compete for ONE slot. Exactly one must succeed.
  const finalQ=await Question.findOne({examId:final._id});
  const concurrent=await Promise.all([0,1].map(()=>fetch(base+'/api/student/exam/'+final._id+'/submit',{method:'POST',headers:{'Content-Type':'application/json',Authorization:'Bearer '+jwt.sign({id:String(student._id)},process.env.JWT_SECRET)},body:JSON.stringify({answers:[{questionId:String(finalQ._id),selectedOptionIndex:1}]})})));
  assert.deepEqual(concurrent.map(r=>r.status).sort(),[200,403]);
  assert.equal((await FinalAuthorization.findById(grant._id)).status,'EXHAUSTED');
  await submit(final,true,403);
  await req(requestPath,'POST'); await req(approvePath,'POST',undefined,admin);
  assert.equal((await FinalAuthorization.findById(grant._id)).attemptLimit,2);
  await req('/api/admin/final-authorizations/'+grant._id+'/revoke','POST',undefined,admin);
  await submit(final,true,403);
  await req(requestPath,'POST'); await req(approvePath,'POST',undefined,admin);
  await submit(final);
  await submit(final,true,403); await req(requestPath,'POST',undefined,student,400);
  assert.equal((await dashboard()).modules[0].status,'COMPLETED');assert.equal((await dashboard()).modules[1].lessons[1].status,'LOCKED');
  assert.equal(await ExamAttempt.countDocuments({examId:final._id}),2);assert.equal(await StudentProgress.countDocuments({lessonId:null}),0);
  await req('/api/admin/exams/'+final._id+'/attempts','GET',undefined,student,403);
  assert.equal((await req('/api/admin/exams/'+final._id+'/attempts','GET',undefined,admin)).length,2);
  await req('/api/admin/exams/'+final._id,'PUT',{status:'INACTIVE'},admin);await req('/api/student/exam/'+final._id,'GET',undefined,student,404);
  assert.equal((await Exam.findById(final._id)).title,final.title);await req('/api/admin/exams/'+final._id,'PUT',{status:'ACTIVE'},admin);
  await req('/api/admin/exams','POST',{title:'Duplicado',moduleId:a._id},admin,400);
  await req('/api/admin/exams','POST',{title:'Ambiguo',moduleId:b._id,lessonId:b1._id},admin,400);
  await req('/api/admin/exams/'+final._id,'PUT',{maxAttemptsPerAuthorization:0},admin,400);
  await req('/api/admin/exams/'+final._id,'PUT',{maxAttemptsPerAuthorization:1.5},admin,400);
  await req('/api/admin/exams/'+final._id,'PUT',{moduleId:b._id},admin,400);
  await req('/api/admin/exams','POST',{title:'Banco sin clase'},admin,201);
  await req('/api/admin/exams','POST',{title:'Otro sin clase'},admin,201);
  await Question.updateMany({examId:exams[0]._id},{$set:{order:20}});
  await Question.updateMany({examId:exams[1]._id},{$set:{order:1}});
  const bank=await req('/api/admin/exams/'+final._id+'/question-bank','GET',undefined,admin);
  assert.equal(bank.length,2);
  assert.deepEqual(bank.map(q=>String(q.sourceLessonId)),[String(a1._id),String(a2._id)]);
  await req('/api/admin/exams/'+exams[0]._id,'PUT',{classModuleId:b._id,lessonId:a1._id},admin,400);
  await req('/api/admin/exams/'+exams[0]._id,'PUT',{classModuleId:a._id,lessonId:a1._id},admin);
  const foreignQ=await Question.findOne({examId:exams[2]._id});
  await req('/api/admin/exams/'+final._id+'/copy-questions','POST',{questionIds:[String(foreignQ._id)]},admin,400);
  const copyPath='/api/admin/exams/'+final._id+'/copy-questions';
  assert.equal((await req(copyPath,'POST',{questionIds:[String(bank[0]._id)]},admin)).copied,1);
  assert.equal((await req(copyPath,'POST',{questionIds:[String(bank[0]._id)]},admin)).copied,0);
  const copy=await Question.findOne({examId:final._id,sourceQuestionId:bank[0]._id});
  await Question.updateOne({_id:bank[0]._id},{$set:{prompt:'Cambio solo en origen'}});
  assert.notEqual((await Question.findById(copy._id)).prompt,'Cambio solo en origen');
  const finalB=await Exam.create({title:'Final independiente',moduleId:b._id,maxAttemptsPerAuthorization:2});
  await Question.create({examId:finalB._id,prompt:'Otra pregunta',options:['Sí','No'],correctOptionIndex:0});
  await req('/api/student/final-exams/'+finalB._id+'/request','POST',undefined,student,400);
  await mark(b1,materials[2]);await submit(exams[2]);await mark(b2,materials[3]);await submit(exams[3]);
  await submit(finalB,true,403);
  await req('/api/student/final-exams/'+finalB._id+'/request','POST');
  assert.equal((await FinalAuthorization.findOne({examId:finalB._id})).status,'PENDING');
  const firstPage = await req('/api/admin/final-authorizations?page=1','GET',undefined,admin);
  assert.equal(firstPage[0].status,'PENDING');
  await req('/api/admin/final-authorizations?page=0','GET',undefined,admin,400);
  for(let i=0;i<5;i++){
    const person=await makeUser('pagination_test_'+i,'STUDENT');
    await FinalAuthorization.create({studentId:person._id,examId:final._id,status:'REVOKED'});
  }
  const page1=await req('/api/admin/final-authorizations?page=1','GET',undefined,admin);
  const page2=await req('/api/admin/final-authorizations?page=2','GET',undefined,admin);
  assert.equal(page1.length,5);assert.equal(page2.length,2);
  assert.equal(new Set([...page1,...page2].map(r=>r._id)).size,7);
  assert.equal((await req('/api/admin/final-authorizations?page=999','GET',undefined,admin)).length,2);
  await submit(finalB,true,403);
  const another=await makeUser('other_student','STUDENT');
  await req('/api/student/exam/'+final._id,'GET',undefined,another,403);
  await req('/api/admin/lessons/'+a2._id,'PUT',{order:1},admin);await mark(a2,materials[1]);await req('/api/student/lesson/'+a1._id,'GET',undefined,student,403);await submit(final,true,403);
  await req('/api/admin/modules/'+b._id,'PUT',{status:'INACTIVE'},admin);await req('/api/student/lesson/'+b1._id,'GET',undefined,student,403);
  await User.updateOne({_id:student._id},{$set:{status:'INACTIVE'}});await req('/api/student/dashboard','GET',undefined,student,403);
  console.log('PASS: real HTTP/MongoDB tests: module independence, materials, GET/POST exam guards, final evaluation, retries, history, admin controls, reordering, inactive accounts/modules.');
  if(process.env.QA_KEEP_OPEN==='true'){
    await User.updateOne({_id:student._id},{$set:{status:'ACTIVE'}});await Module.updateOne({_id:b._id},{$set:{status:'ACTIVE'}});
    console.log('Browser fixtures ready at '+base+'; admin_test / student_test, password Local-test-only-2026!');
    await new Promise(resolve=>{process.once('SIGINT',resolve);process.once('SIGTERM',resolve);});
  }
}finally{if(server)await new Promise(r=>server.close(r));await mongoose.disconnect();await mongo.stop();}
